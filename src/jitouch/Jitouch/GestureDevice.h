// Defines the engine device identifiers shared by gesture dispatch and checks.

#ifndef GestureDevice_h
#define GestureDevice_h

enum {
    TRACKPAD = 0,
    MAGICMOUSE = 1,
    CHARRECOGNITION = 2,
};

#endif
